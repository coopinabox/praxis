"use strict";

var React = require('react');

module.exports = React.createClass({
  getInitialState: function () {
    return {
      brand: require('utils').version,
    };
  },
  render: require('./Header.jsx'),
});
