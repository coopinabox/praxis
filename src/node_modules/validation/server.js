"use strict";

// add validation to bookshelf
var sql = require('sql');
var validation = require('backbone-validation');
var _ = require('underscore');
_.extend(sql.Model.prototype, validation.mixin);
