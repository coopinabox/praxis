"use strict";

var bb = require('bb');

require('validation/client');

module.exports = bb.Model.extend({
  defaults: {
    name: "",
    description: "",
  },

  initialize: function () {
    var self = this;
    this.on('change', function () {
      self.validate();
    });
  },

  validation: require('tasks-common/validation'),
});
