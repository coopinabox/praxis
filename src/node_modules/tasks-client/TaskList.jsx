var React = require('react');

module.exports = function (taskViews) {
  return (
    <div className="tasks">
      <header>
        <h2>tasks</h2>
        <span className="btn-group">
          <button className="add" onClick={this.add} type="button">add</button>
        </span>
      </header>
      <main>
        { taskViews }
      </main>
    </div>
  );
};
