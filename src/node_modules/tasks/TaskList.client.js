"use strict";

var bb = require('bb');

var Task = require('./Task.client');

module.exports = bb.Collection.extend({
  model: Task,
  url: '/tasks',
  initialize: function () {
    this.fetch();
  },
});
