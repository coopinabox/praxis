
var holons = require('../')

describe('holon service', function () {
  describe('.create', function () {
    it("should create a new object", function (done) {
      holons.create({}, function (err, holon) {
        err.not.ok;
        holon.ok
      })
    })
  })
})
